spring:
  datasource:
    url: jdbc:mysql://localhost/coviddb?serverTimezone=UTC
    username: covid
    password: 041829

  jpa:
    show_sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        format_sql: true

logging.level:
  org.hibernate.SQL: debug
  org.hibernate.type: trace


